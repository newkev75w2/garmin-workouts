# Garmin session tokens - never commit these
.garmin_session/

# Your actual lifted weights/reps pulled from Garmin. Personal training data —
# kept local by default since this repo is public. Delete this line if you'd
# rather version it.
performance.json

# Python
__pycache__/
*.pyc
.venv/

.DS_Store
.pytest_cache/

# Cached wger exercise index (regenerate with demos.refresh_index) and the
# generated preview pages. Both are derived data, not source.
wger_index.json
previews/
