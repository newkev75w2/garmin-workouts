# Garmin session tokens - never commit these
.garmin_session/

# Your actual lifted weights/reps pulled from Garmin. Personal training data —
# kept local by default since this repo is public. Delete this line if you'd
# rather version it.
performance.json

# Python
__pycache__/
*.pyc
.venv/

.DS_Store
.pytest_cache/

# Cached sleep/readiness metrics — personal, and re-fetchable
recovery.json

# Synced runs and VO2max series — personal, re-fetchable
runs.json

# Built skill package — regenerate with `python package_skill.py`
dist/

# Drawn route pages — regenerated on demand
routes/
